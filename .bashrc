#!/bin/bash

# get here
alias aa='vim ~/.bashrc'
alias rr='source ~/.bashrc'

# colors
source ~/.colors

# functions
source ~/.functions
alias ff='winpty nvim ~/.functions'

# cd commands
alias cd..='cd ..'
alias dev='cd ~/Developer'
alias dd='cd ~/Developer'
alias nn='cd ~/AppData/Local/nvim'
alias jj='cd ~/Developer/jelly-bean'

# basic
alias la='ls -a'
alias ll='ls -l'
alias c='clear'

# git
alias gs='git status'
alias pp='git push'
alias add='git add -u'

# nvim
alias vim='winpty nvim'
alias nvim='winpty nvim'
alias vv='winpty nvim'

# PS1
# todo put ps1 edits here

